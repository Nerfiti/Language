var GLOBAL 0

define GetPrevNumber(num)
{
    return num-1
}

define main()
{
    var number 1
    var fact call factorial(number+2)

    {
        call print(fact)
    }

    while (fact != 1 || 1 < 0)
    {
    	fact = call GetPrevNumber(fact)
    }
    
    if (a == fact && 1 >= 1)
    {
        fact = (fact + 1) * 1 / 1
    }
    else
    {
        fact = fact - 1
    }

    return fact + call GetMinusOne()
}

define GetMinusOne()
{
    return !-5 - 1
}